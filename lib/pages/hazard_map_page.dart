import 'package:flutter/material.dart';

class HazardMapPage extends StatelessWidget {
  const HazardMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Hazard Map Page", style: TextStyle(fontSize: 20)),
    );
  }
}
